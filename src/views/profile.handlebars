<section class="vh-100 container" style="background-color: #eee;">
    <div class="container h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-lg-12 col-xl-11">
                <div class="card text-black" style="border-radius: 25px;">
                    <div class="card-body p-md-5">
                        <div class="row justify-content-center">
                            <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">

                                <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">Perfil</p>

                                <div class="d-flex flex-row align-items-center mb-4">
                                    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                    <div class="form-outline flex-fill mb-0">
                                        <label class="form-label" for="form3Example1c">Adjunte foto de perfil</label>
                                        <form id="registerForm" action="/api/users/{{user._id}}/documents/profiles"
                                            enctype="multipart/form-data" method="POST">
                                            <input type="file" name="profile">
                                            <input type="submit" value="Upload a file...">
                                        </form>
                                    </div>
                                </div>

                                <div class="d-flex flex-row align-items-center mb-4">
                                    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                    <div class="form-outline flex-fill mb-0">
                                        <label class="form-label" for="form3Example1c">Adjunte documentos de
                                            identificacion </label>
                                        <form action="/api/users/{{user._id}}/documents/identificacion"
                                            enctype="multipart/form-data" method="POST">
                                            <input type="file" name="identificacion">
                                            <input type="submit" value="Upload a file...">
                                        </form>
                                    </div>
                                </div>

                                <div class="d-flex flex-row align-items-center mb-4">
                                    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                    <div class="form-outline flex-fill mb-0">
                                        <label class="form-label" for="form3Example1c">Adjunte comprobante de domicilio
                                        </label>
                                        <form action="/api/users/{{user._id}}/documents/domicilio"
                                            enctype="multipart/form-data" method="POST">
                                            <input type="file" name="domicilio">
                                            <input type="submit" value="Upload a file...">
                                        </form>
                                    </div>
                                </div>

                                <div class="d-flex flex-row align-items-center mb-4">
                                    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                    <div class="form-outline flex-fill mb-0">
                                        <label class="form-label" for="form3Example1c">Adjunte comprobante de estado de
                                            cuenta </label>
                                        <form action="/api/users/{{user._id}}/documents/cuenta"
                                            enctype="multipart/form-data" method="POST">
                                            <input type="file" name="cuenta">
                                            <input type="submit" value="Upload a file...">
                                        </form>
                                    </div>
                                </div>

                                <div class="d-flex flex-row align-items-center mb-4">
                                    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                    <div class="form-outline flex-fill mb-0">
                                        <label class="form-label" for="form3Example1c">Adjunte foto de productos </label>
                                        <form action="/api/users/{{user._id}}/documents/products"
                                            enctype="multipart/form-data" method="POST">
                                            <input type="file" name="product">
                                            <input type="submit" value="Upload a file...">
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

